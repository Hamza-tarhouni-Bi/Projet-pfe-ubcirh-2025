import { BaseComponent } from "tw-elements-react/dist/types/types/baseComponent";
import { placement } from "tw-elements-react/dist/types/types/placement";
import { Boundary } from "tw-elements-react/node_modules/@popperjs/core";
interface PopoverContentTheme {
    popoverContent?: string;
    fade?: string;
}
interface PopoverContentProps extends BaseComponent {
    popperTag?: React.ComponentProps<any>;
    theme?: PopoverContentTheme;
    container?: string | boolean;
    placement?: placement;
    popperConfig?: Record<string, unknown>;
    offset?: [number, number];
    fallbackPlacements?: placement[];
    boundary?: Boundary;
}
export { PopoverContentProps };
